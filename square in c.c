#include<stdio.h>
void main()
{
	int a, b, c;
	a=2;
	b=a*a;
	c=b*a;
	printf("area %d of a square with side is %d:", a, b );
	printf("area %d and perimeter of a square with side is %d:", b, c);	
}
